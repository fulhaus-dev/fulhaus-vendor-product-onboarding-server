export default async function processSpreadsheetFileProductDataStream(args: {
	spreadsheetFileStream: NodeJS.ReadableStream;
	vendorProductDataR2FolderName: string;
	fileName: string;
	ownerId: string;
}) {
	// const { spreadsheetFileStream, vendorProductDataR2FolderName, fileName, ownerId } = args;
	// TODO: Implement rest of this function
}
